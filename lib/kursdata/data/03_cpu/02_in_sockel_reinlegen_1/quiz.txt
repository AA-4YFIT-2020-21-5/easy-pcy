Warum sind die zwei Dreiecke so wichtig?
✔ Damit die CPU funktioniert, müssen beide übereinstimmen.
x Damit die CPU funktioniert, müssen beide dialog gegenüber sein
x Damit die CPU funktioniert, müssen beide parallel sein
x Es hat keine große Bedeutung